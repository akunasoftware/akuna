[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "akuna-core"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
bindings = "uniffi"
module-name = "akuna_core"
